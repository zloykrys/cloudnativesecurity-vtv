apiVersion: neuvector.com/v1
kind: NvAdmissionControlSecurityRule
metadata:
  name: local
spec:
  rules:
    - id: 10001
      action: allow
      comment: "Allow explicitly signed SUSE AppCo MariaDB images"
      criteria:
        # FIXED: Admission Control requires list-based operators like "containsAny"
        - name: namespace
          op: containsAny 
          value: mariadb-secure
        - name: imageVerifiers
          op: containsAny
          value: suse-appco/suse-appco-key

    - id: 10002
      action: deny
      comment: "Default Deny: Block all unverified images in this namespace"
      criteria:
        # FIXED: Operator updated to "containsAny"
        - name: namespace
          op: containsAny
          value: mariadb-secure